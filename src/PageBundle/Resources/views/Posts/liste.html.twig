{% extends "PageBundle:Default:index.html.twig" %}

{% block block_css %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="{{ asset("bundles/page/css/sliderPost.css") }}">
{% endblock %}
{% block slider_js %}
    {{ parent() }}
    <script src="{{ asset("bundles/page/js/Script/sliderPost.js") }}" type="text/javascript"></script>
{% endblock %}



{% block body %}
    {#<div class="global">#}
        {#<div id="galerie">#}
            {#<div class="slider">#}
                {#{% for post in posts %}#}
                    {#<div class="slider-content slider-post">#}
                        {#<div class="content-post">#}
                            {#<p>{{ post.title }}</p>#}
                            {#<p>posté le : {{ post.date|date("d/m/Y") }}</p>#}
                            {#Condition si une image est présente dans l'article#}
                            {#{% if post.media == true %}#}
                                {#<img src="{{ asset(post.media.webPath) }}" alt="image de {{ post.title }}">#}
                            {#{% endif %}#}
                            {#<p>{{ post.content }}</p>#}
                            {#<div class="button">Voir</div>#}
                        {#</div>#}
                    {#</div>#}
                {#{% endfor %}#}
            {#</div>#}
            {#<i class="medium right chevron icon orange suiv"></i>#}
            {#<i class="medium left chevron icon orange prec"></i>#}
        {#</div>#}
    <div id="carrousel-post">
        {% for post in posts %}
            <div class="content-post-slider">
                <div class="post-title">
                    <div class="date">{{ post.date|date("d/m/Y") }}</div>
                    <div class="title">{{ post.title }}</div>
                </div>
                {#Condition si une image est présente dans l'article#}
                <div class="post-content">
                    {% if post.media == true %}
                        <img src="{{ asset(post.media.webPath) }}" alt="image de {{ post.title }}">
                    {% endif %}
                    <p>{{ post.content }}</p>
                </div>
            </div>
        {% endfor %}
        <i class="fa fa-angle-left fa-3x prev" aria-hidden="true"></i>
        <i class="fa fa-angle-right fa-3x next" aria-hidden="true"></i>
    </div>
    {#</div>#}
{% endblock %}

{% block javascript %}
    {{ parent() }}
    {#<script src="{{ asset("bundles/page/js/Script/sliderPost.js") }}" type="text/javascript"></script>#}
{% endblock %}
