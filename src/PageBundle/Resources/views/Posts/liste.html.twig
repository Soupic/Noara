{% extends "PageBundle::layout.html.twig" %}

{% block body %}
    {#<div class="slideshow-title">{{ race.name }}</div>#}
    {#<div class="slidesho-contentw">{{ race.content }}</div>#}
    {#<div>#}
    {#<img src="{{ asset(race.media.webPath) }}" alt="image de {{ race.name }}" class="blk-img">#}
    {#</div>#}
    {#<ul>#}
    {#<li><img src="{{ asset(race.media.webPath) }}" alt="image de {{ race.name }}"></li>#}
    {#<li>{{ race.name }}</li>#}
    {#<li>{{ race.content }}</li>#}
    {#</ul>#}
    <div class="global">
        <div id="galerie">
            <div class="slider">
                {% for post in posts %}
                    <div class="slider-content">
                        <img src="{{ asset(post.media.webPath) }}" alt="image de {{ post.title }}">
                        <div class="content-race">
                            <p>{{ post.content }}</p>
                            <p>{{ post.title }}</p>
                            <div class="button">Voir</div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <i class="medium right chevron icon orange suiv"></i>
            <i class="medium left chevron icon orange prec"></i>
        </div>
    </div>
{% endblock %}